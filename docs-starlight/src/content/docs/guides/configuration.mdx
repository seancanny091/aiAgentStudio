---
title: Configuration
description: Complete reference for agent and capability settings.
---

import { Aside, Badge, Card, CardGrid, LinkButton, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

# Configuration Guide

Complete reference for all agent and capability settings.

<Aside type="note" title="Use This Page As Your Admin Baseline">
Start with agent + provider settings, then tune safety and performance as usage grows.
</Aside>

<CardGrid>
  <Card title="Agent Settings" icon="seti:settings">Identity, memory, behavior, and runtime controls.</Card>
  <Card title="Trust & Safety" icon="seti:lock">PII masking and prompt safety thresholds.</Card>
  <Card title="Capability Design" icon="seti:tools">Implementation type, schemas, and execution rules.</Card>
</CardGrid>

<p><Badge text="Admin" variant="note" size="small" /> <Badge text="Security Critical" variant="caution" size="small" /> <Badge text="Reference" variant="default" size="small" /></p>

<Steps>
1. Configure core agent and LLM settings.
2. Enable trust and safety controls.
3. Define capability contracts and execution policies.
4. Validate in sandbox before production rollout.
</Steps>

<LinkButton href="../troubleshooting/" variant="secondary">Troubleshooting Guide</LinkButton>
<LinkButton href="../../reference/security/" icon="right-arrow">Security Reference</LinkButton>


## Agent Configuration

### Basic Settings

| Field | Type | Description |
|:------|:-----|:------------|
| `Name` | Text | Display name for the agent |
| `DeveloperName__c` | Text | Unique API identifier (no spaces) |
| `AgentType__c` | Picklist | Conversational, Function, Workflow, or Email |
| `IsActive__c` | Checkbox | Enable/disable the agent |

### AI Provider Settings

| Field | Type | Description |
|:------|:-----|:------------|
| `LLMConfiguration__c` | Lookup | Which AI provider configuration to use |
| `MemoryStrategy__c` | Picklist | How to manage conversation history |
| `HistoryTurnLimit__c` | Number | Number of conversation turns to remember |

### Memory Strategies

<CardGrid>
  <Card title="BufferWindow" icon="seti:clock">
    Keeps the last N turns verbatim. Best for short conversations where precise recent context matters.
  </Card>
  <Card title="SummaryBuffer" icon="seti:text">
    Summarizes older turns while keeping recent ones verbatim. Best for long conversations and cost optimization.
  </Card>
</CardGrid>

### Behavior Settings

| Field | Type | Description |
|:------|:-----|:------------|
| `IdentityPrompt__c` | Long Text | Defines who the agent is (persona) |
| `InstructionsPrompt__c` | Long Text | How the agent should behave |
| `EnableActionTransparency__c` | Checkbox | Show tool execution details to users |
| `EnableToolReasoning__c` | Checkbox | Require LLM to explain tool selection for better transparency |
| `AuditLevel__c` | Picklist | None, Standard, or Detailed logging |
| `EnableDependencyValidation__c` | Checkbox | Enforce tool dependency graph at runtime |
| `ToolDependencyGraph__c` | Long Text | JSON dependency graph (approved) |
| `EnableNextStepSuggestion__c` | Checkbox | Injects `_nextStepSuggestion` into tools (experimental) |

### Performance Settings

| Field | Type | Description |
|:------|:-----|:------------|
| `AsyncDispatchType__c` | Picklist | High (Platform Events) or Low (Queueables) |
| `EnableParallelToolCalling__c` | Checkbox | Execute multiple tools simultaneously |
| `MaxProcessingCycles__c` | Number | Max LLM cycles per execution |

### Service User Context (Optional)

| Field | Type | Description |
|:------|:-----|:------------|
| `RequiresServiceUserContext__c` | Checkbox | Route execution through service user context via REST callout |
| `ServiceUserNamedCredential__c` | Text | Named Credential for loopback callouts |

### Trust & Safety

| Field | Type | Description |
|:------|:-----|:------------|
| `PIIMaskingPreset__c` | Picklist | Off, Standard, or Strict |
| `PromptSafetyPreset__c` | Picklist | Off or Standard — delegates to the provider adapter's native safety pre-check |

---

## Async Dispatch Types

<CardGrid>
  <Card title="High — Platform Events" icon="rocket">
    **Best for**: Production environments with many concurrent users.

    - Handles thousands of simultaneous conversations
    - Event-driven, highly scalable architecture
    - Better throughput under governor limits
    - Harder to debug (no synchronous log trail)
  </Card>
  <Card title="Low — Queueables" icon="seti:gear">
    **Best for**: Development, testing, and debugging.

    - Sequential processing with full debug log support
    - Guaranteed execution order
    - Easy to trace in Developer Console
    - Limited concurrent executions
  </Card>
</CardGrid>

---

## LLM Configuration

| Field | Type | Description |
|:------|:-----|:------------|
| `DeveloperName__c` | Text | Unique identifier |
| `NamedCredential__c` | Text | Named Credential API name |
| `ProviderAdapterClass__c` | Text | Apex class for provider integration |
| `DefaultModelIdentifier__c` | Text | Model identifier (e.g., gpt-4o) |
| `DefaultTemperature__c` | Number | Creativity level (0.0 - 1.0) |
| `IsActive__c` | Checkbox | Enable this configuration |

### Temperature Guide

<CardGrid>
  <Card title="0.0 – 0.3 · Deterministic" icon="seti:config">
    Focused, consistent output. Use for data retrieval, classification, and structured extraction where predictability matters.
  </Card>
  <Card title="0.4 – 0.7 · Balanced" icon="approve-check">
    Natural-sounding responses with reasonable variety. The right default for general-purpose assistants.
  </Card>
  <Card title="0.8 – 1.0 · Creative" icon="star">
    Varied, imaginative output. Use for content generation, brainstorming, and conversational personas where diversity is desirable.
  </Card>
</CardGrid>

---

## Capability Configuration

### Basic Info

| Field | Type | Description |
|:------|:-----|:------------|
| `CapabilityName__c` | Text | Tool name shown to AI (use snake_case) |
| `Description__c` | Long Text | When and how to use this tool |
| `ImplementationType__c` | Picklist | Standard, Apex, or Flow |

### Implementation Types

<CardGrid>
  <Card title="Standard" icon="seti:settings">
    Built-in actions for common Salesforce operations: GetRecordDetails, CreateRecord, UpdateRecord, FlowHandler, PostChatter, and more.
  </Card>
  <Card title="Apex" icon="seti:apex">
    Custom Apex class implementing `IAgentAction`. Full access to Salesforce data, callouts, and complex business logic.
  </Card>
  <Card title="Flow" icon="seti:flow">
    Salesforce Flow invocation. Ideal for no-code automation reuse. Set the Flow API name in `ImplementationDetail__c`.
  </Card>
</CardGrid>

### Execution Settings

| Field | Type | Description |
|:------|:-----|:------------|
| `HITLMode__c` | Picklist | Human-in-the-Loop mode: blank (no HITL), Confirmation (LLM asks in chat), Approval (formal approval process), or ConfirmationThenApproval (both) |
| `HITLNotificationPreference__c` | Picklist | Controls when to send notifications for HITL actions: "Always Notify" (default) sends notifications for approvals, rejections, and errors; "Notify on Rejection Only" only sends notifications when actions are rejected. Only applies when `HITLMode__c` is "Approval" or "ConfirmationThenApproval". |
| `RunAsynchronously__c` | Checkbox | Execute in separate transaction |
| `ExposureLevel__c` | Picklist | External (visible to LLM), Internal (framework only), or Disabled |

### Configuration Fields

| Field | Type | Description |
|:------|:-----|:------------|
| `BackendConfiguration__c` | Long Text | Admin settings (JSON) |
| `Parameters__c` | Long Text | Tool parameters (JSON Schema) |

---

## Writing Effective Descriptions

The capability description is crucial for the AI to understand when to use a tool.

<Tabs>
  <TabItem label="Good example">

```
Search for contacts in Salesforce by name, email, or account.
Use this capability when:
- User asks to find a contact
- User wants contact information
- User mentions a person's name in a business context

Do NOT use when:
- User is asking about accounts (use search_accounts instead)
- User wants to create a new contact (use create_contact instead)
```

  </TabItem>
  <TabItem label="Bad example">

```
Gets contacts
```

Vague descriptions cause the LLM to misuse or ignore the capability. Always specify **when to use**, **when not to use**, and the **expected inputs**.

  </TabItem>
</Tabs>

---

## JSON Schema for Parameters

Parameters use [JSON Schema](https://json-schema.org/) format.

<Tabs>
  <TabItem label="Basic">

```json
{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The contact's full name"
    }
  }
}
```

  </TabItem>
  <TabItem label="With required fields">

```json
{
  "type": "object",
  "required": ["email"],
  "properties": {
    "email": {
      "type": "string",
      "description": "Email address (required)"
    },
    "name": {
      "type": "string",
      "description": "Optional name filter"
    }
  }
}
```

  </TabItem>
  <TabItem label="With enums">

```json
{
  "type": "object",
  "properties": {
    "priority": {
      "type": "string",
      "enum": ["High", "Normal", "Low"],
      "description": "Task priority level"
    }
  }
}
```

  </TabItem>
  <TabItem label="Complex">

```json
{
  "type": "object",
  "required": ["objectType"],
  "properties": {
    "objectType": {
      "type": "string",
      "enum": ["Account", "Contact", "Opportunity"],
      "description": "Salesforce object to search"
    },
    "filters": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "field": {"type": "string"},
          "operator": {"type": "string", "enum": ["=", "!=", "LIKE"]},
          "value": {"type": "string"}
        }
      },
      "description": "Search filters to apply"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 10,
      "description": "Maximum records to return"
    }
  }
}
```

  </TabItem>
</Tabs>

---

## Backend Configuration Examples

<Tabs>
  <TabItem label="GetRecords">

```json
{
  "objectApiName": "Contact",
  "defaultFields": ["Id", "Name", "Email", "Phone", "Account.Name"],
  "defaultLimit": 25
}
```

  </TabItem>
  <TabItem label="CreateRecord">

```json
{
  "objectApiName": "Task",
  "defaultValues": {
    "OwnerId": "{!$User.Id}",
    "Status": "Not Started"
  }
}
```

  </TabItem>
  <TabItem label="Flow">

For Flow implementation, set `ImplementationType__c` to `Flow` and put the Flow API name in `ImplementationDetail__c`.

```json
{
  "defaultInputValues": {
    "source": "AI_Agent"
  }
}
```

  </TabItem>
</Tabs>
